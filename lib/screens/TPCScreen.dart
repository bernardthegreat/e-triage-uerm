import 'package:flutter/material.dart';

class TPCScreen extends StatelessWidget {
  const TPCScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Third Party Contractors Here'),
    );
  }
}